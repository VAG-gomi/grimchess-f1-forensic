# Source Origin Map

`src/` is copied from the frozen `GrimChess-Current-Forensic-Instrumented/grimchess-mobile` workspace. Its detailed provenance is recorded by the included baseline and F1 instrumentation manifests. `android/` contains the corresponding Android project inputs. `build-config/` contains reproducibility-critical configuration copies. `forensic/` contains provenance documentation and manifests. `tests/` contains controlled harness artifacts and is not live-device evidence. `artifacts/` contains the frozen F1 binary.
